# Q. Write a program which store the user inputed name and also,
# give the length of user's inputed name.

name   = input("Enter your name : ")
length = len(name)
print(f"The length of your name : '{name}' is {length}.")